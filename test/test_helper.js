// These are intentionally left as a NOOP so that Google Closure can be used
// when compiling the sources, but it is ignored when running tests.
goog = {
  require: function() { },
  provide: function() { }
}
